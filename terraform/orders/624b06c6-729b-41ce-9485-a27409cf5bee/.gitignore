# Terraform state — never commit
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Variable files containing secrets
terraform.tfvars
*.auto.tfvars

# Plan output files (binary and JSON — may contain sensitive variable values)
*.tfplan
tfplan
tfplan.json
tfplan.txt
