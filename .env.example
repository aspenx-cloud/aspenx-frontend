# Copy this file to .env.local and fill in your values.
# Never commit .env.local to git.

# Firebase — from your Firebase project console
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# API Gateway base URL (no trailing slash)
# Defaults to the custom domain if not set
VITE_API_BASE_URL=https://api.aspenx.cloud

# Vite base path (optional — defaults to /aspenx-frontend/)
# Set to / when deploying to a custom domain
# VITE_BASE_PATH=/
